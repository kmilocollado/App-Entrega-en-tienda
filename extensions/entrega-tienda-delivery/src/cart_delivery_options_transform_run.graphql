query CartDeliveryOptionsTransformRunInput {
  cart {
    billingAddress {
      city
      provinceCode
      zip
      countryCode
    }
    deliveryGroups {
      deliveryAddress {
        city
        provinceCode
        zip
        countryCode
      }
      deliveryOptions {
        handle
        title
        deliveryMethodType
        cost {
          amount
        }
      }
    }
  }
  shop {
    appEntregaConfig: metafield(namespace: "$app", key: "entrega_tienda_config") {
      jsonValue
    }
    legacyEntregaConfig: metafield(namespace: "custom", key: "entrega_tienda_config") {
      jsonValue
    }
  }
}
